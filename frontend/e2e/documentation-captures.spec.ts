import path from "node:path";import {expect,test} from "@playwright/test";
const captures=path.resolve(process.cwd(),"../docs/capturas");
test.beforeEach(({},testInfo)=>{test.skip(testInfo.project.name!=="chromium-desktop","Las capturas se generan una sola vez en Chromium de escritorio.");});
test("genera la captura del catálogo público",async({page})=>{const fair={id:"f1",nombre:"Feria Productiva Boliviana",descripcion:"Productos elaborados por Unidades Productivas de todo el país.",ubicacion:"Campo Ferial",fecha_inicio:"2026-07-21",fecha_fin:"2026-07-24",estado:"PUBLISHED",imagen_portada:null};await page.route("**/api/public/fairs/active",r=>r.fulfill({json:{active:true,fair,items:[fair]}}));await page.goto("/catalogo");await expect(page.getByRole("heading",{name:"Feria Productiva Boliviana"})).toBeVisible();await page.screenshot({path:path.join(captures,"catalogo-publico.png"),fullPage:true});});
test("genera la captura del panel administrativo",async({page})=>{const user={id:"admin-1",username:"admin",email:"admin@gmail.com",first_name:"Ana",last_name:"Admin",role:"ADMIN",must_change_password:false};await page.addInitScript(session=>{localStorage.setItem("catalog_token","jwt-captura");localStorage.setItem("catalog_user",JSON.stringify(session));},user);await page.route("**/api/auth/me",r=>r.fulfill({json:user}));await page.route("**/api/admin/dashboard",r=>r.fulfill({json:{stats:{ferias:3,ferias_publicadas:1,productos:18,productos_disponibles:14,unidades_productivas:7,unidades_productivas_activas:6,solicitudes_pendientes:2,participaciones_pendientes:1},recent_audits:[{id:"a1",accion:"APROBAR",entidad:"Solicitud",descripcion:"Solicitud de Manos Bolivianas aprobada",usuario:"admin",created_at:"2026-07-22T10:30:00-04:00"}]}}));await page.goto("/admin");await expect(page.getByRole("heading",{name:"Resumen administrativo"})).toBeVisible();await page.screenshot({path:path.join(captures,"panel-administrativo.png"),fullPage:true});});
