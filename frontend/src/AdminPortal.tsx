import{useState}from'react';
import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';
import{Link,NavLink,useNavigate}from'react-router-dom';
import{Activity,CalendarDays,FolderTree,LayoutDashboard,LogOut,Menu,ShieldCheck,Store,Users,X}from'lucide-react';
import{api}from'./api';

const nav=[['/gestion/admin/dashboard','Resumen',LayoutDashboard],['/gestion/admin/administradores','Administradores',Users],['/gestion/admin/expositores','Expositores',Store],['/gestion/admin/ferias','Ferias',CalendarDays],['/gestion/admin/categorias','Categorías',FolderTree],['/gestion/admin/auditoria','Auditoría',Activity]] as const;
export function AdminLayout({children}:{children:React.ReactNode}){const[open,setOpen]=useState(false);const navigate=useNavigate();return <div className="min-h-screen bg-stone-100"><header className="fixed inset-x-0 top-0 z-30 h-16 bg-brand-800 text-white"><div className="flex h-full items-center justify-between px-5"><button className="md:hidden" onClick={()=>setOpen(true)}><Menu/></button><strong>Catálogo Digital de Ferias</strong><button className="flex items-center gap-2" onClick={()=>{localStorage.clear();navigate('/gestion/login')}}><LogOut size={18}/>Salir</button></div></header><aside className={`fixed bottom-0 left-0 top-0 z-40 w-72 bg-white p-5 shadow-xl transition-transform md:top-16 md:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><button className="ml-auto block md:hidden" onClick={()=>setOpen(false)}><X/></button><p className="mb-5 flex items-center gap-2 font-bold text-brand-800"><ShieldCheck/>Portal de gestión</p><nav className="space-y-1">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-4 py-3 ${isActive?'bg-brand-600 text-white':'text-stone-700 hover:bg-stone-100'}`}><Icon size={19}/>{label}</NavLink>)}</nav><Link to="/catalogo" className="mt-8 block px-4 text-sm text-brand-600">Ver catálogo público</Link></aside><main className="pt-16 md:pl-72"><div className="mx-auto max-w-7xl p-6">{children}</div></main></div>}
const Loading=()=> <div className="animate-pulse space-y-3"><div className="h-8 w-52 rounded bg-stone-200"/><div className="h-32 rounded-2xl bg-stone-200"/></div>;
const ErrorBox=({text}:{text:string})=><p className="rounded-xl bg-red-50 p-4 text-red-700">{text}</p>;

export function AdminDashboard(){const{data,isLoading,error}=useQuery({queryKey:['admin-dashboard'],queryFn:()=>api.get('/admin/dashboard').then(r=>r.data)});if(isLoading)return <AdminLayout><Loading/></AdminLayout>;if(error)return <AdminLayout><ErrorBox text="No se pudo cargar el dashboard."/></AdminLayout>;const cards=[['Ferias',data.stats.ferias],['Publicadas',data.stats.ferias_publicadas],['Expositores activos',data.stats.expositores_activos],['Productos',data.stats.productos],['Disponibles',data.stats.productos_disponibles],['Sin stock',data.stats.productos_sin_stock],['Asignaciones pendientes',data.stats.asignaciones_pendientes]];return <AdminLayout><h1 className="text-3xl font-bold">Resumen administrativo</h1><p className="mt-2 text-stone-600">Indicadores actualizados desde PostgreSQL.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label,value])=><article className="card" key={String(label)}><p className="text-sm text-stone-500">{label}</p><strong className="mt-2 block text-3xl text-brand-800">{value}</strong></article>)}</div><section className="card mt-6"><h2 className="text-xl font-bold">Actividad reciente</h2>{data.recent_audits.length===0?<p className="mt-4 text-stone-500">Todavía no existen acciones registradas.</p>:<div className="mt-4 divide-y">{data.recent_audits.map((a:any)=><div className="py-3" key={a.id}><strong>{a.accion}</strong> · {a.entidad}<p className="text-sm text-stone-500">{a.descripcion||'Sin descripción'}</p></div>)}</div>}</section></AdminLayout>}

export function AdministratorsPage(){const qc=useQueryClient();const[form,setForm]=useState({first_name:'',last_name:'',email:'',phone:'',cargo:'',unidad:'',role:'ADMIN_VICEMINISTERIO'});const[credentials,setCredentials]=useState<any>(null);const{data,isLoading}=useQuery({queryKey:['admins'],queryFn:()=>api.get('/admin/users').then(r=>r.data.items)});const create=useMutation({mutationFn:()=>api.post('/admin/users',form),onSuccess:r=>{setCredentials(r.data);setForm({...form,first_name:'',last_name:'',email:'',phone:'',cargo:'',unidad:''});qc.invalidateQueries({queryKey:['admins']})}});return <AdminLayout><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-3xl font-bold">Administradores</h1><p className="text-stone-600">Cuentas encargadas del Viceministerio.</p></div></div>{credentials&&<div className="mt-5 rounded-xl border border-amber-300 bg-amber-50 p-4"><strong>Credenciales temporales — se muestran una sola vez</strong><p>Usuario: {credentials.data.username}</p><p>Contraseña: {credentials.temporary_password}</p><button className="mt-2 text-sm underline" onClick={()=>setCredentials(null)}>Ya las guardé</button></div>}<form className="card mt-6 grid gap-4 md:grid-cols-2" onSubmit={e=>{e.preventDefault();create.mutate()}}><h2 className="text-xl font-bold md:col-span-2">Nuevo administrador</h2>{Object.entries(form).map(([key,value])=>key==='role'?<label key={key}>Rol<select className="input mt-1" value={value} onChange={e=>setForm({...form,role:e.target.value})}><option value="ADMIN_VICEMINISTERIO">Administrador</option><option value="SUPERADMIN">Superadministrador</option></select></label>:<label key={key}>{({first_name:'Nombres',last_name:'Apellidos',email:'Gmail',phone:'Teléfono',cargo:'Cargo',unidad:'Unidad'} as any)[key]}<input required={['first_name','last_name','email'].includes(key)} className="input mt-1" value={value} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}{create.error&&<ErrorBox text={(create.error as any).response?.data?.error||'No se pudo crear.'}/>}<button disabled={create.isPending} className="btn md:col-span-2">{create.isPending?'Guardando…':'Crear administrador'}</button></form><section className="card mt-6 overflow-x-auto">{isLoading?<Loading/>:data?.length===0?<p>No hay administradores.</p>:<table className="w-full text-left"><thead><tr className="border-b"><th className="p-3">Nombre</th><th>Gmail</th><th>Rol</th><th>Estado</th></tr></thead><tbody>{data?.map((u:any)=><tr className="border-b" key={u.id}><td className="p-3">{u.first_name} {u.last_name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.status}</td></tr>)}</tbody></table>}</section></AdminLayout>}

export function FairsPage(){const qc=useQueryClient();const[form,setForm]=useState({nombre:'',descripcion:'',lugar:'',departamento:'La Paz',municipio:'',fecha_inicio:'',fecha_fin:'',imagen_portada:''});const{data,isLoading}=useQuery({queryKey:['fairs-admin'],queryFn:()=>api.get('/fairs').then(r=>r.data.items)});const create=useMutation({mutationFn:()=>api.post('/fairs',form),onSuccess:()=>{qc.invalidateQueries({queryKey:['fairs-admin']});setForm({...form,nombre:'',descripcion:'',lugar:'',municipio:'',fecha_inicio:'',fecha_fin:'',imagen_portada:''})}});const change=useMutation({mutationFn:({id,status}:{id:string,status:string})=>api.patch(`/fairs/${id}/status`,{status}),onSuccess:()=>qc.invalidateQueries({queryKey:['fairs-admin']})});return <AdminLayout><h1 className="text-3xl font-bold">Gestión de ferias</h1><form className="card mt-6 grid gap-4 md:grid-cols-2" onSubmit={e=>{e.preventDefault();create.mutate()}}><h2 className="text-xl font-bold md:col-span-2">Nueva feria</h2>{Object.entries(form).map(([key,value])=><label key={key}>{({nombre:'Nombre',descripcion:'Descripción',lugar:'Lugar',departamento:'Departamento',municipio:'Municipio',fecha_inicio:'Fecha inicial',fecha_fin:'Fecha final',imagen_portada:'URL de portada'} as any)[key]}<input type={key.startsWith('fecha')?'date':'text'} required={key!=='descripcion'} className="input mt-1" value={value} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}{create.error&&<ErrorBox text={(create.error as any).response?.data?.error||'No se pudo crear.'}/>}<button disabled={create.isPending} className="btn md:col-span-2">Crear feria</button></form><div className="mt-6 grid gap-5 lg:grid-cols-2">{isLoading?<Loading/>:data?.length===0?<p className="card">No existen ferias. Cree la primera.</p>:data?.map((f:any)=><article className="card" key={f.id}><img className="h-40 w-full rounded-xl object-cover" src={f.imagen_portada} alt=""/><div className="mt-4 flex items-start justify-between"><div><h2 className="text-xl font-bold">{f.nombre}</h2><p>{f.municipio}, {f.departamento}</p><p className="text-sm text-stone-500">{f.fecha_inicio} — {f.fecha_fin}</p></div><span className="rounded-full bg-stone-100 px-3 py-1 text-xs">{f.estado}</span></div><div className="mt-4 flex gap-2">{f.estado!=='PUBLISHED'&&<button className="btn" onClick={()=>change.mutate({id:f.id,status:'PUBLISHED'})}>Publicar</button>}{f.estado==='PUBLISHED'&&<button className="rounded-xl border border-red-300 px-4 py-2 text-red-700" onClick={()=>change.mutate({id:f.id,status:'DISABLED'})}>Inhabilitar</button>}</div></article>)}</div></AdminLayout>}

export function CategoriesPage(){const qc=useQueryClient();const[name,setName]=useState('');const{data,isLoading}=useQuery({queryKey:['categories-admin'],queryFn:()=>api.get('/admin/categories').then(r=>r.data.items)});const create=useMutation({mutationFn:()=>api.post('/categories',{nombre:name}),onSuccess:()=>{setName('');qc.invalidateQueries({queryKey:['categories-admin']})}});const toggle=useMutation({mutationFn:(c:any)=>api.patch(`/categories/${c.id}/status`,{active:!c.estado}),onSuccess:()=>qc.invalidateQueries({queryKey:['categories-admin']})});return <AdminLayout><h1 className="text-3xl font-bold">Categorías</h1><form className="card mt-6 flex gap-3" onSubmit={e=>{e.preventDefault();create.mutate()}}><input required className="input" placeholder="Nombre de categoría" value={name} onChange={e=>setName(e.target.value)}/><button className="btn">Agregar</button></form><section className="card mt-6">{isLoading?<Loading/>:<div className="divide-y">{data?.map((c:any)=><div className="flex items-center justify-between py-3" key={c.id}><div><strong>{c.nombre}</strong><p className="text-sm text-stone-500">{c.estado?'Activa':'Inactiva'}</p></div><button className="rounded-lg border px-3 py-2" onClick={()=>toggle.mutate(c)}>{c.estado?'Inhabilitar':'Reactivar'}</button></div>)}</div>}</section></AdminLayout>}

export function AuditPage(){const{data,isLoading}=useQuery({queryKey:['audit'],queryFn:()=>api.get('/audit').then(r=>r.data.items)});return <AdminLayout><h1 className="text-3xl font-bold">Auditoría</h1><section className="card mt-6 overflow-x-auto">{isLoading?<Loading/>:data?.length===0?<p>No existen registros.</p>:<table className="w-full text-left"><thead><tr className="border-b"><th className="p-3">Fecha</th><th>Acción</th><th>Entidad</th><th>Descripción</th></tr></thead><tbody>{data?.map((a:any)=><tr className="border-b" key={a.id}><td className="p-3">{new Date(a.created_at).toLocaleString('es-BO')}</td><td>{a.accion}</td><td>{a.entidad}</td><td>{a.descripcion}</td></tr>)}</tbody></table>}</section></AdminLayout>}

export function ExhibitorsPage(){const qc=useQueryClient();const empty={nombre_comercial:'',tipo_documento:'CI',numero_documento:'',nombre_responsable:'',apellido_responsable:'',correo:'',telefono_whatsapp:'',departamento:'La Paz',municipio:'',direccion:'',descripcion:'',descripcion_productos:'',logo:'',type_ids:[] as string[]};const[form,setForm]=useState(empty);const[credentials,setCredentials]=useState<any>(null);const[q,setQ]=useState('');const{data,isLoading}=useQuery({queryKey:['exhibitors',q],queryFn:()=>api.get('/exhibitors',{params:{q}}).then(r=>r.data.items)});const{data:types=[]}=useQuery({queryKey:['exhibitor-types'],queryFn:()=>api.get('/exhibitor-types').then(r=>r.data.items)});const create=useMutation({mutationFn:()=>api.post('/exhibitors',form),onSuccess:r=>{setCredentials(r.data);setForm(empty);qc.invalidateQueries({queryKey:['exhibitors']})}});return <AdminLayout><h1 className="text-3xl font-bold">Expositores</h1>{credentials&&<div className="mt-5 rounded-xl border border-amber-300 bg-amber-50 p-4"><strong>Credenciales temporales — se muestran una sola vez</strong><p>Usuario: {credentials.username}</p><p>Contraseña: {credentials.temporary_password}</p><button className="mt-2 underline" onClick={()=>setCredentials(null)}>Ya las guardé</button></div>}<form className="card mt-6 grid gap-4 md:grid-cols-2" onSubmit={e=>{e.preventDefault();create.mutate()}}><h2 className="text-xl font-bold md:col-span-2">Nuevo expositor</h2>{Object.entries(form).filter(([k])=>k!=='type_ids').map(([key,value])=><label key={key}>{({nombre_comercial:'Nombre comercial',tipo_documento:'Tipo de documento',numero_documento:'Número de documento',nombre_responsable:'Nombre del responsable',apellido_responsable:'Apellido del responsable',correo:'Correo',telefono_whatsapp:'WhatsApp',departamento:'Departamento',municipio:'Municipio',direccion:'Dirección',descripcion:'Descripción',descripcion_productos:'Productos que ofrece',logo:'Logo'} as any)[key]}{key==='tipo_documento'?<select className="input mt-1" value={value as string} onChange={e=>setForm({...form,tipo_documento:e.target.value})}><option>CI</option><option>NIT</option><option>OTRO</option></select>:<input required={!['direccion','descripcion','descripcion_productos','logo'].includes(key)} className="input mt-1" value={value as string} onChange={e=>setForm({...form,[key]:e.target.value})}/>}</label>)}<fieldset className="md:col-span-2"><legend className="font-medium">Tipos de expositor</legend><div className="mt-2 flex flex-wrap gap-3">{types.map((t:any)=><label className="rounded-lg border px-3 py-2" key={t.id}><input type="checkbox" className="mr-2" checked={form.type_ids.includes(t.id)} onChange={e=>setForm({...form,type_ids:e.target.checked?[...form.type_ids,t.id]:form.type_ids.filter(id=>id!==t.id)})}/>{t.nombre}</label>)}</div></fieldset>{create.error&&<ErrorBox text={(create.error as any).response?.data?.error||'No se pudo crear el expositor.'}/>}<button disabled={create.isPending} className="btn md:col-span-2">{create.isPending?'Creando…':'Crear expositor y cuenta'}</button></form><section className="card mt-6"><input className="input mb-4" placeholder="Buscar por nombre, correo o documento" value={q} onChange={e=>setQ(e.target.value)}/>{isLoading?<Loading/>:data?.length===0?<p>No se encontraron expositores.</p>:<div className="overflow-x-auto"><table className="w-full text-left"><thead><tr className="border-b"><th className="p-3">Nombre comercial</th><th>Responsable</th><th>WhatsApp</th><th>Ubicación</th><th>Estado</th></tr></thead><tbody>{data?.map((e:any)=><tr className="border-b" key={e.id}><td className="p-3 font-medium">{e.nombre_comercial}</td><td>{e.nombre_responsable} {e.apellido_responsable}</td><td>+{e.telefono_whatsapp}</td><td>{e.municipio}, {e.departamento}</td><td>{e.estado}</td></tr>)}</tbody></table></div>}</section></AdminLayout>}
